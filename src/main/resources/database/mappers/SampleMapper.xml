<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="samle">
    <insert id="a"></insert>
    <update id="b"></update>
    <delete id="c"></delete>
    <select id="d"></select>
  <!-- sql문  -->
  <!-- 값이 비어있으면 에러 -->
  </mapper>
  